# LLM Configuration - Choose ONE option below

# Option 1: Use Ollama (Local, Free, Fast) - RECOMMENDED
USE_OLLAMA=true
OLLAMA_MODEL=llama3
OLLAMA_BASE_URL=http://localhost:11434

# Option 2: Use Hugging Face (Cloud API)
# USE_OLLAMA=false
# HF_API_KEY=your_huggingface_api_key_here
# HF_MODEL_URL=https://api-inference.huggingface.co/models/meta-llama/Meta-Llama-3-8B-Instruct

# Email Configuration (Gmail SMTP)
SENDER_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your_gmail_app_password_here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# Application Configuration
LOG_LEVEL=INFO
OUTPUT_DIR=/tmp/ai_audit_reports

# Note: For Gmail, you need to generate an "App Password"
# 1. Go to Google Account Settings
# 2. Security -> 2-Step Verification (enable it)
# 3. App Passwords -> Generate a new app password
# 4. Use that password in SMTP_PASSWORD
